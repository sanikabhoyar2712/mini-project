import React from "react";
import NotesList from "../components/NotesList";

const Notes = () => {
    return ( <
        div >
        <
        h1 > All Notes < /h1> <
        NotesList / >
        <
        /div>
    );
};

export default Notes;